import React from 'react';
import { expect } from 'chai';
import Basetable from './BaseTable';

describe('Spec Basetable', function () {
    it('it exists', () => {
        expect(Basetable).to.be.ok;
    });
});
