import React from 'react';
import { expect } from 'chai';
import Siteheader from './SiteHeader';

describe('Spec Siteheader', function () {
    it('it exists', () => {
        expect(Siteheader).to.be.ok;
    });
});
